# The Pup Café — Dynamic Financial Model
Live financial model, assumptions file, auto‑generated PDFs, and a public dashboard.

**Workflow**
1. Edit numbers in `data/assumptions.yaml`
2. git add .  → git commit → git push
3. GitHub Actions rebuilds PDFs and dashboard
